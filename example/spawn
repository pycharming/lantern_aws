#!/bin/bash

if test -z $1; then echo "Usage: spawn <node name>" >&2 ; exit 1; fi

cd $(dirname $0)

../bin/spawn.py $1 client_secrets.json user_credentials.json env-vars.txt bns_cert.p12 bns-osx-cert-developer-id-application.p12
