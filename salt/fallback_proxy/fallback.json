{"ip" : "{{ grains.get('ec2_public-ipv4', None) or grains['ipv4'][1] }}",
 "port" : "{% if pillar['proxy_protocol'] == 'tcp' %}443{% else %}{{ grains['proxy_port'] }}{% endif %}",
 "auth_token" : "{{ pillar['auth_token'] }}",
 "protocol" : "{{ pillar['proxy_protocol'] }}"
}
 
